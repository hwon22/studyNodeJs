
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Hello World!</title>
    </head>
    <body>
        <h1>Hello
            <% if(name){ %>
                <%= name %>!
            <% } else { %>
                Stranger!</span>
            <% } %>
        </h1>
    </body>
</html>